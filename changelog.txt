changelog — perf_advisor
========================

2026-04-14
----------
- README.md: remove Testing section; move "Preparing a profile" content into a new "Profiles" subsection under Usage; add tested Nsight Systems versions (2025.3.1.90, 2025.5 via HPC SDK 25.5, 2025.5.2).
- perf_advisor/agent/loop.py (_run_gemini): implement explicit context caching for the Gemini backend. Before the first turn, pre-create a named CachedContent (TTL 600 s) containing the system instruction, tool declarations, and pre-computed profile summary. Every subsequent turn references this cache, billing the fixed prefix at 0.25× rather than full input rate. Falls back transparently to injecting the summary into the first user message if cache creation fails. Updated _add_gemini_usage to accumulate cache_read_tokens from cached_content_token_count; updated all four return sites to pass _cache_creation_tokens / cache_read_tokens instead of zeros. Updated verbose display to show cached fraction as a percentage. Updated logger to record cache name and token count on turn 1.
- perf_advisor/agent/preflight.py: add estimate_gemini_cache_breakdown() — estimates cache_write, cache_read, non-cached input, and cost-equivalent tokens for a Gemini explicit-cache session (creation at 1.0×, reads at 0.25×).
- perf_advisor/__main__.py: add Gemini-specific pre-flight estimate block (cached prefix / cache reads / non-cached / cost-equiv); add estimate_gemini_cache_breakdown to cmd_analyze imports; use provider-appropriate cache billing rates in the end-of-run token summary (Gemini: 1.0×/0.25×, Anthropic: 1.25×/0.10×); propagate provider key through token_usage dict.
- perf_advisor/agent/loop.py (run_agent): add token_usage["provider"] = resolved_provider so __main__.py can select the correct cost-equiv formula.
- README.md: replace "Google Gemini — not implemented" with full description of the explicit-cache implementation; add Gemini pre-flight example; update minimizing-token-expenditure section to note Gemini caching is now active.
- paper/perf_advisor.tex: add §3.3.3 Gemini explicit context caching with cost model equation and savings estimate; fix gemini-2.0-flash → gemini-2.5-flash in §3.1. Recompiled to presentation/draft_5.pdf.
- perf_advisor/agent/loop.py, perf_advisor/__main__.py, README.md: replace gemini-2.0-flash (unavailable) with gemini-2.5-flash as the default Gemini model; update README example from gemini-1.5-pro to gemini-2.5-pro.
- perf_advisor/agent/loop.py: add _gemini_text() helper to extract text from Gemini responses by reading candidates[0].content.parts directly, avoiding the SDK warning about non-text parts (function_call) that fired during logging and verbose display.
- README.md: add runtime_fraction_pct, estimated_speedup_pct_lower/upper, and confidence to the Stage 2 hypothesis field list.
- paper/perf_advisor.tex: add four new hypothesis fields (runtime_fraction_pct, estimated_speedup_pct_lower/upper, confidence) to §2.1.4; replace "rich table" with stacked-panel description; update §3.2 cache structure from two permanent markers to one (pre-seed checkpoint implicitly covers tools + system + preseed); fix §4.1 NVTX annotation claim (annotations were removed from benchmark to avoid biasing the agent); fix §5.1 to clarify hypothesis JSON output requires --log; fix §5.2 multi-rank CLI (no --multi-rank flag, all profiles are positional args); update §6 Impact cache-marker description to match. Recompiled to 14 pages (presentation/draft_4.pdf).
- CLAUDE.md: update current draft pointer to draft_4.pdf.
- perf_advisor/agent/loop.py (_run_api): consolidate permanent prompt-cache markers from 2 to 1; remove cache_control from the system prompt block when pre-seeding is active (the single marker on the last pre-seed tool result implicitly caches tools + system + preseed as one unit, covering the same bytes); fall back to a system-prompt marker when summary is None; frees the fourth allowed marker as a reserve. Update cache strategy comment accordingly.
- perf_advisor/analysis/cross_rank.py, diff.py, metrics.py, models.py, tests/test_cross_rank.py: ruff format pre-existing style fixes (no logic changes).
- perf_advisor/agent/loop.py (_run_api): guard against empty tool_results before appending the user message; if stop_reason is not "end_turn" but no tool_use blocks were emitted (e.g. max_tokens truncation), inject a "Please continue." text block to prevent Anthropic API 400 "user messages must have non-empty content".
- perf_advisor/analysis/models.py: add HypothesisReport Pydantic model (profile_path, generated_at, provider, model, hypotheses list); add DeviceInfo Pydantic model (name, compute_capability, sm_count, max_threads_per_sm, peak_memory_bandwidth_GBs, total_memory_GiB, l2_cache_MiB, max_threads_per_block, max_registers_per_block, shared memory limits, clock_rate_MHz); add device_info: DeviceInfo field to ProfileSummary.
- perf_advisor/analysis/metrics.py: expand compute_device_info() to populate the full DeviceInfo model from TARGET_INFO_GPU (all 12 hardware properties); change return type from dict to DeviceInfo; update all internal call sites (compute_top_kernels, _window_top_kernels, compute_phase_summary) to use attribute access; wire device_info into the ProfileSummary returned by compute_profile_summary().
- perf_advisor/agent/loop.py: add _format_device_context() helper that formats a compact "## Target Hardware" section from a DeviceInfo; update _build_system_prompt() and _format_summary_prompt() to accept and inject device_info; wire device_info from summary.device_info into all four backends (Anthropic, OpenAI, Gemini, claude_code) and the pre-flight token estimator.
- tests/test_synthetic.py: fix two tests that accessed compute_device_info() return value as a dict (now use attribute access on DeviceInfo model).
- perf_advisor/__main__.py (cmd_analyze): when --log is requested, write {profile_stem}_{ts}_hypotheses.json next to the log file containing a HypothesisReport (metadata + full hypothesis list); path is printed alongside the LLM log path.
- perf_advisor/analysis/models.py: add PhaseDiff model (per-phase scalar diffs for phase_aware compare mode); extend ProfileDiff with cpu_sync_blocked_s / cpu_sync_blocked_pct (nullable ScalarDiff), stream_count_a/b (int), dominant_stream_pct_a/b (nullable float), and phase_diffs list[PhaseDiff].
- perf_advisor/analysis/diff.py: add _sdiff_opt() helper for nullable scalar diffs; compute cpu_sync_blocked_s/pct diffs, stream topology (count + dominant stream %) for both profiles, and per-phase diffs (populated only in phase_aware mode) in compute_profile_diff().
- perf_advisor/analysis/models.py: add phase: str = "whole_profile" field to ComparisonDiff so the LLM can attribute each key_difference to a specific execution phase or the overall profile.
- perf_advisor/agent/compare.py: update _COMPARE_SYSTEM_PROMPT JSON schema to include the phase field with instructions to use exact phase names from phase_diffs or "whole_profile"; reinforce in phase_aware mode description.
- perf_advisor/__main__.py (cmd_compare): add Phase column to the Key Differences rich table; renders as dim "—" for whole_profile rows and cyan phase name for phase-specific findings.
- README.md: document hardware properties extraction in the Stage 1 section; update --log description to mention the companion hypotheses JSON; update the Saved Files risk entry to list all three output files and their naming convention.

- paper/literature_review.md: new file — literature survey covering 11 papers on LLM-assisted HPC optimization and agentic code analysis found via live web search; entries organized by theme (LLM HPC evaluation, profiling-guided LLM optimization, agentic kernel generation, project-level LLM optimization, benchmarks).
- paper/perf_advisor.bib: add 11 new bibliography entries (cui2025llmperf, astra2025, pragma2025, opal2025, gigiprofiler2025, kernelbench2025, cudallm2025, kernelmep2025, alphaevolve2025, kernelfoundry2026, polo2025) to support the expanded related-work discussion.
- paper/perf_advisor.tex: expand Motivation and Significance section with a new paragraph surveying the surge of agentic LLM kernel-optimization work (Astra, PRAGMA, CUDA-LLM, Opal, POLO, MEP, AlphaEvolve, KernelFoundry, KernelBench, Cui et al.), and a second paragraph on profiling-guided diagnosis (gigiProfiler); sharpens PerfAdvisor's positioning as a system-level diagnosis tool rather than a kernel code generator; recompiled to 14 pages (presentation/draft_3.pdf).
- paper/perf_advisor.tex: add \\usepackage[T1]{fontenc} to preamble to support the Croatian \\dj glyph in the Opal author name (Inđić).
- perf_advisor/agent/loop.py: extend _HYPOTHESIS_SCHEMA with four new fields: runtime_fraction_pct (fraction of phase wall-clock time attributable to this bottleneck, agent computes from profile data), estimated_speedup_pct_lower / estimated_speedup_pct_upper (Amdahl-derived speedup range for partial vs. full mitigation), and confidence (high/medium/low tied to evidence quality: directly visible vs. derived vs. plausible).
- perf_advisor/__main__.py (cmd_analyze): replace the flat hypotheses rich table with stacked rich Panels (one per hypothesis); each panel has a one-line header bar (index, type, phase, impact, action, confidence, runtime%, speedup) and a three-section body (Description / Evidence / Suggestion); panel border color follows impact level (red/yellow/blue); Panel and Text imported at module level, removing duplicate local import from _print_cross_rank_tables.

2026-04-13
----------
- paper/perf_advisor.tex, paper/perf_advisor.bib, paper/perf_advisor.pdf: add SoftwareX/JOSS-style draft paper introducing PerfAdvisor; covers motivation, architecture, benchmark suite design, and evaluation results (100% bottleneck detection, 54% Haiku / 71% Opus suggestion coverage across 8 scenarios).
- paper/perf_advisor.tex, paper/perf_advisor.pdf: revise paper structure — add Section 3 "Prompt Engineering and Cost Management" covering system prompt design (grounding constraint, untrusted-data note, action_category boundary rule, SQLite schema reference, behavioral guards) and the sliding-window prompt caching strategy (~80% token cost reduction over uncached sessions); fold the synthetic benchmark suite into a new top-level "Quality Evaluation" section where it is a subsection rather than a standalone section.
- bench/, perf_advisor/eval/discover.py: rename all benchmark run IDs from run_NN to test_NN (run_01–04 → test_01–04; run_11–14 → test_05–08); updated ground_truth_meta.json run_id fields, submit_1gpu/4gpu/8gpu.sbatch profile names, README.md headings and cross-references; filesystem files in bench/profiles/ renamed to match.
- perf_advisor/agent/tools.py: fix tool_top_kernels — occupancy and launch-overhead fields were null when called with start_ns/end_ns time bounds because device_info and launch_overhead were never computed in that code path; now computed once and passed to both _window_top_kernels and compute_top_kernels; same fix applied to tool_phase_summary, which also failed to pass these to compute_phase_summary.
- perf_advisor/analysis/phases.py: fix _label — reserved "idle" for phases with zero GPU kernel activity (kernel_ns == 0); previously a phase with 2.6% GPU utilization but 150,000 kernel calls was labeled "idle" because the threshold was gpu_util < 5.0%; now falls through to the dominant-kernel name, so launch-overhead-dominated phases are labeled by their kernel rather than misleadingly labeled as idle.
- perf_advisor/agent/tools.py, loop.py: clarify get_table_schema semantics in both the tool description and the system prompt — wording now says "use this *instead of* SELECT * LIMIT 1; never issue SELECT * just to discover columns" to prevent the pattern of calling both get_table_schema and a SELECT * query for the same table.
- perf_advisor/eval/report.py: improve --verbose run header; now shows expected_bottleneck=<label> detected_bottleneck=<advisor type> ✓/✗ instead of the previous terse detection=✓ format, making it clear which bottleneck type PerfAdvisor actually reported.
- bench/README.md: add "Evaluating PerfAdvisor" section documenting `perf-advisor evaluate` usage (full eval, rescore from cache, skip-judge, verbose, multi-model examples) and the scoring table.
- perf_advisor/eval/__init__.py, discover.py, scorer.py, report.py: add evaluation harness package; discover_runs() walks profiles/{1gpu,4gpu,8gpu}/ and resolves sqlite paths; score_run() applies two-tier scoring (enum+keyword bottleneck detection, LLM-judge suggestion coverage 0/1/2 per expected suggestion); print_summary_table() renders aggregate detection accuracy, coverage, and false-positive count; save/load_results() for incremental output and --cached rescore mode.
- perf_advisor/analysis/metrics.py, phases.py: fix "list index out of range" on kernel-less profiles (run_11/run_14) — compute_profile_span, _window_kernel_time, and compute_gpu_kernel_time now guard against missing CUPTI_ACTIVITY_KIND_KERNEL with has_table() checks; the UNION ALL queries in compute_profile_span and _profile_bounds (phases.py) now build the source list conditionally so a missing kernel table doesn't collapse the whole query; _collect_candidate_boundaries now skips GPU-edge tagging when the kernel table is absent.
- perf_advisor/ingestion/profile.py: fix crash on profiles with no CUDA kernels (run_11/run_14 halo-exchange scenarios) — NsysProfile.query() now returns [] instead of raising OperationalError when a table referenced in SQL does not exist (e.g. CUPTI_ACTIVITY_KIND_KERNEL absent in kernel-less MPI profiles); NsysProfile.columns() now returns [] instead of raising ValueError for missing tables, consistent with its use as a feature-detection pattern in metrics.py.
- perf_advisor/agent/loop.py: fix misleading reason string in _parse_provider_and_model — when a bare model ID is passed via --model and the provider is auto-detected from an API key, the reason now reads "--model flag (provider auto-detected from ANTHROPIC_API_KEY)" instead of "presence of ANTHROPIC_API_KEY".
- perf_advisor/__main__.py: add `evaluate` subcommand; add --log/--log-file (per-run LLM interaction logs, --log-file takes a directory) and --transcript/--transcript-file (single session transcript) mirroring the analyze subcommand; accepts profiles_dir positional, --ground-truth, --model (hypothesis generation), --judge-model (default haiku), --output (incremental JSON), --cached (rescore without re-running PerfAdvisor), --skip-judge, --allow-app-knowledge, --verbose, --yes; handles single-GPU and multi-rank profiles using the same cross-rank analysis path as `analyze`.
- bench/ground_truth_meta.json: create static evaluation rubric for all 8 benchmark scenarios; keyed by the scenario name written into runtime ground-truth JSONs; each entry records display_name, expected_bottleneck, nsys_signature (observable timeline clues), and suggestions (3 concrete action/mechanism/rationale triples); used by PerfAdvisor to score hypothesis quality beyond simple bottleneck-label matching.
- bench/run_analysis.md: create critical analysis of all benchmark runs (runs 01–16); each entry examines whether the stated kernels and parameters are used, whether the bottleneck is induced realistically, whether Nsight Systems profiles show an unambiguous signature, and what improvements or alternatives exist; cross-cutting issues section covers Nsight Systems vs. Nsight Compute gaps, code deduplication, missing reference runs, and the unused `force_serialize` flag.
- bench/run_analysis.md: add Recommendation subsection to each run classifying it as keep / keep-with-tweak / improve, with concrete change instructions (parameter adjustments, new runs to add, code fixes, README corrections).
- bench/submit_1gpu.sbatch, bench/README.md: remove run_05 (HBM bandwidth-bound), run_06 (uncoalesced access), and run_08 (low SM occupancy) — all three bottlenecks require Nsight Compute hardware counters to diagnose and produce no unambiguous signal in Nsight Systems profiles alone.
- bench/submit_8gpu.sbatch, bench/README.md: remove run_16 (GPU-Direct RDMA) — it is the optimized baseline with no deficiency visible in Nsight Systems.
- bench/submit_1gpu.sbatch: run_01 — add --host-sleep-us 5 to guarantee driver-independent inter-kernel gaps; run_03/04 — raise --work-iters from 2 to 128 so the compute stage (~25 µs/chunk) is visible alongside PCIe transfers (~250 µs/chunk).
- bench/submit_4gpu.sbatch: run_11 — reduce --transfer-size-mb from 512 to 64 to match inter-node runs; run_12 — remove --stagger-us 1000 (artificial CPU sleep) and raise --n to 16777216 so the 4× GPU compute spread between ranks is the sole source of load imbalance.
- bench/synthetic_cuda_benchmark.cu: remove force_serialize from Config, parse_args, sync condition, ground-truth JSON, and CSV output (was dead code — overlap_missing serialization is driven by the dispatch-level argument, not this flag); in run_transfer_bound, initialize dev.b once before the rep loop instead of re-copying it per chunk per pass (was a spurious extra H2D per chunk).
- bench/README.md: update run_03/04 descriptions to reflect --work-iters 128; update run_11 to 64 MB; update run_12 to remove stagger mention; fix run_13 description ("1 kernel with 4 inner FMA iterations per element", not "4 compute iterations").
- perf_advisor/__main__.py: add `_FullHelpParser` subclass so `perf-advisor -h` prints full option details for all subcommands (analyze, compare, summary), not just their names.
- perf_advisor/ingestion/profile.py: remove 50,000-row cap and truncation warning from `NsysProfile.query()` — internal analysis callers (phases.py, metrics.py) are trusted and need unrestricted access; LLM-instigated queries go through `query_safe()` which already enforces a 200-row limit.
- perf_advisor/analysis/phases.py: remove 50,000-row cap and warning from `_mpi_cluster_boundaries()` — raw rows are consumed internally and reduced to a list of phase-boundary timestamps; the LLM only ever receives the final bounded `PhaseSummary` models.

2026-04-10
----------
- bench/submit_1gpu.sbatch, bench/README.md: remove run_07 (SFU saturation — too narrow/specialized for typical HPC users) and run_09 (low occupancy severe — redundant with run_08).
- bench/submit_8gpu.sbatch, bench/README.md: remove run_15 (inter-node host-staged — identical workload to run_14, differed only in ground-truth label).
- perf_advisor/analysis/phases.py: reduce merge-nearby tolerance floor from 50ms to 5ms — the old floor was dominating for sub-second profiles (7% of a 719ms span), risking collapse of real phase boundaries.
- perf_advisor/analysis/phases.py: add identical-phase pre-pass (step 5a) — merges adjacent segments unconditionally when they share the same dominant kernel and GPU utilization differs by less than 15%, looping until stable; the existing budget-reduction loop becomes step 5b and runs afterward.
- perf_advisor/analysis/phases.py: raise gap outlier multiplier from 10 to 50 (via new _GAP_OUTLIER_MULTIPLIER constant) to reduce sensitivity on dense-kernel profiles; move gap outlier detection fully into SQL — median and threshold filter are now computed server-side so only outlier rows are returned, avoiding the 50K row truncation warning on large profiles; remove unused `statistics` import.
- perf_advisor/analysis/phases.py: replace rolling-average merge-nearby with priority-based discard — each candidate boundary is now tagged with a source priority (GPU edge > NVTX > MPI cluster > gap midpoint); when two candidates fall within tolerance the lower-priority one is dropped rather than the pair being averaged into a new synthetic timestamp, preventing boundaries from drifting into adjacent regions.
- perf_advisor/analysis/phases.py: replace fixed top-N GPU idle gap selection with outlier-based filtering — only gaps exceeding max(10 × median_gap, 1000 ns) are returned as candidates; profiles with uniformly small inter-kernel gaps now produce no gap-derived boundaries instead of always injecting N candidates.
- bench/synthetic_cuda_benchmark.cu: redesign P2P scenarios (scen_h, scen_i) to use N symmetric MPI ranks instead of a single process with two GPUs — each rank now participates in a ring; scen_h uses CUDA IPC handles exchanged via MPI_Sendrecv for direct GPU-to-GPU copies (NVLink path); scen_i delegates to run_halo_exchange_impl(device_ptrs=false) for explicit D2H→MPI_Sendrecv→H2D; add P2pNvlink and P2pStaged to requires_mpi().
- bench/submit_4gpu.sbatch: replace run_p2p helper (srun -n 1 --gpus=2 --gpu-bind=none) with profile_mpi for runs #10–#11; all 4 intra-node ranks now participate symmetrically; update header comment.
- bench/synthetic_cuda_benchmark.cu: fix scen_h (p2p_nvlink) — replace CUDA IPC ring with CUDA-aware MPI (device pointers via MPI_Sendrecv, same as scen_n); cudaIpcOpenMemHandle fails with CUDA_ERROR_INVALID_VALUE when the peer memory is on a different physical GPU — IPC only works for same-device cross-process sharing; with --gpus-per-task=1 each rank owns a different physical GPU so IPC is not usable; GTL routes intra-node CUDA-aware MPI via NVLink, which is the correct mechanism.
- bench/submit_4gpu.sbatch: add MPICH_GPU_SUPPORT_ENABLED=1 and MPICH_RDMA_ENABLED_CUDA=1 (required for GTL linking and CUDA-aware MPI); add --cuda-aware-mpi 1 to run_10.
- bench/submit_8gpu.sbatch: add --distribution=cyclic — interleaves ranks across nodes (rank 0 → node 0, rank 1 → node 1, …) so every adjacent pair in the halo ring crosses the Slingshot network; without this, 6 of 8 ring pairs were intra-node and GTL attempted CUDA IPC for those pairs, which fails with CUDA_ERROR_INVALID_VALUE because --gpus-per-task=1 gives each rank a distinct physical GPU, causing MPI_Sendrecv to hang.
- bench/submit_4gpu.sbatch: drop run_10 (scen_h, intra-node GPU-Direct NVLink) — true NVLink between MPI processes requires CUDA IPC, which cannot work when each rank owns a different physical GPU; GTL has no usable fallback for this case on Perlmutter.
- bench/README.md: add benchmark documentation — intro, build/submit instructions, and per-run reference with bottleneck description and resolution advice for all 15 runs.
- bench/synthetic_cuda_benchmark.cu: remove all NVTX annotations (NvtxRange struct, USE_NVTX include, every range push/pop) — NVTX strings appear verbatim in profile data and would give PerfAdvisor direct hints about the bottleneck being tested.
- bench/synthetic_cuda_benchmark.cu: rename all kernels to neutral identifiers (kernel_a…kernel_e) with descriptive comments in the source; kernel names are visible in profiles and descriptive names (e.g. compute_heavy_kernel) would bias PerfAdvisor's LLM.
- bench/synthetic_cuda_benchmark.cu: replace descriptive --scenario names (e.g. compute_bound) with opaque IDs (scen_a…scen_n); nsys captures the full process argv so the scenario name would appear in profile metadata; to_string() still returns descriptive names for ground-truth JSON and stdout only.
- bench/build.sh: remove -DUSE_NVTX from NVCCFLAGS (NVTX removed from source).
- bench/Makefile: remove nvtx and mpi_nvtx targets (now identical to all/mpi).
- bench/submit_1gpu.sbatch, submit_4gpu.sbatch, submit_8gpu.sbatch: replace --scenario <name> with opaque scen_X IDs; update run comments to describe the bottleneck in plain English (visible to user in script, not in profile); drop nvtx from nsys -t flags (binary emits no NVTX events).
- bench/synthetic_cuda_benchmark.cu: fix run_low_occupancy for --smem-kb > 48 — validate against sharedMemPerBlockOptin (164 KB on A100) instead of sharedMemPerBlock (default 48 KB limit), and call cudaFuncSetAttribute(cudaFuncAttributeMaxDynamicSharedMemorySize) to opt kernel_e into the larger carveout before launch.
- bench/submit_1gpu.sbatch, submit_4gpu.sbatch, submit_8gpu.sbatch: neutralize run IDs (run_01…run_16) — the ID is passed to nsys -o and --ground-truth, so it appears in profile metadata (TARGET_INFO_SESSION) and process argv where the agent's sql_query tool could read it; descriptive names like comp, mem_coal, rdma_gpu would reveal the bottleneck type.


- bench/build.sh, bench/Makefile: remove -lnvToolsExt — NVTX3 (nvtx3/nvToolsExt.h) is header-only and libnvToolsExt was removed in CUDA 12.x; the source already included the NVTX3 header so no source changes were needed.
- bench/build.sh: link GTL (GPU Transfer Library) — Cray MPICH aborts MPI_Init with "GTL library is not linked" when MPICH_GPU_SUPPORT_ENABLED=1 is set in the environment (Perlmutter default) and the binary lacks -lmpi_gtl_cuda; detect via Cray PE per-accelerator variables PE_MPICH_GTL_DIR_${CRAY_ACCEL_TARGET} and PE_MPICH_GTL_LIBS_${CRAY_ACCEL_TARGET} (e.g. nvidia80 on Perlmutter A100) using bash indirect expansion; mpicxx -show does not include these flags automatically.
- bench/submit_1gpu.sbatch, bench/submit_4gpu.sbatch: move P2P runs (#10–#11) from submit_1gpu to submit_4gpu — P2P requires ≥2 GPUs so it doesn't belong in the single-GPU script; submit_4gpu adds a run_p2p helper using srun -n 1 --gpus=2 --gpu-bind=none to expose two GPUs to the single process; submit_1gpu now covers only runs #1–#9 with --gpu-bind=map_gpu:0.
- bench/submit_4gpu.sbatch, bench/submit_8gpu.sbatch: created Slurm sbatch scripts for intra-node multi-GPU (runs #10–#13) and inter-node MPI (8 ranks, runs #14–#16); no email directives.
- bench/submit_1gpu.sbatch: removed email directives (#SBATCH --mail-user, --mail-type).

2026-04-09
----------
- bench/build.sh: add Perlmutter build script — loads cudatoolkit module, checks for nvcc/mpicxx, compiles MPI+NVTX binary (sm_80/A100); supports --arch and --debug flags; prints usage reminder on success.
- bench/synthetic_cuda_benchmark.cu: remove atomic_contention scenario — Nsight Systems captures no per-SM atomic-throughput counters; detecting atomic serialization requires Nsight Compute, so the scenario cannot be evaluated by PerfAdvisor and would produce misleading ground-truth labels. Removed Scenario::AtomicContention, atomic_hist_kernel, fill_atomic_keys, --atomic-bins/--hot-bin-span knobs, DeviceArrays::keys/bins, and all related CSV/JSON fields.
- bench/synthetic_cuda_benchmark.cu: fix atomic_contention timing — cudaMemset moved before cudaEventRecord(start) so bin-reset is not charged to the measured kernel time.
- bench/synthetic_cuda_benchmark.cu: fix event recording asymmetry in memory_bandwidth and compute_bound — cudaEventRecord(start) is now unconditional (matching stop); elapsed_ms is still guarded by the timed flag.
- bench/synthetic_cuda_benchmark.cu: remove dead cfg.blocks field (--blocks arg); all scenarios compute their own block count and never used it.
- bench/synthetic_cuda_benchmark.cu: add sync_stall scenario — launches tiny_kernel then immediately calls cudaStreamSynchronize(0) per iteration, producing a CPU-stalled sawtooth pattern that PerfAdvisor should detect as cpu_sync_stall.
- bench/synthetic_cuda_benchmark.cu: add low_occupancy scenario — uses dynamic shared memory (--smem-kb, default 48 KB) to limit blocks-per-SM and reduce warp occupancy; validates smem_bytes >= threads*sizeof(float) and against device limit.
- bench/synthetic_cuda_benchmark.cu: add --ground-truth PATH option; writes a JSON file with scenario name, expected_bottleneck label, all params, and timing result for automated PerfAdvisor eval.
- bench/synthetic_cuda_benchmark.cu: add clarifying comments on compute_bound (SFU saturation, not FP32/tensor) and memory_bandwidth (stride=1 = HBM bandwidth, stride>1 = uncoalesced access).
- bench/Makefile: add Makefile with 'all' (no NVTX) and 'nvtx' (DUSE_NVTX + lnvToolsExt) targets.
- bench/synthetic_cuda_benchmark.cu: add MPI infrastructure — MpiCtx struct, init_mpi (MPI_Comm_split_type for local rank), finalize_mpi, barrier_sync, reduce_time_max; all scenario runners now accept MpiCtx and insert barrier before start and MPI_MAX reduce on timing. Device auto-assigned from MPI local_rank when size>1.
- bench/synthetic_cuda_benchmark.cu: add mpi_barrier_stall scenario — each rank does work_iters*(rank+1) SFU iterations + rank*stagger_us CPU sleep then MPI_Barrier; tests load-imbalance detection.
- bench/synthetic_cuda_benchmark.cu: add mpi_allreduce scenario — compute → D2H → MPI_Allreduce → H2D; tests host-staged collective detection.
- bench/synthetic_cuda_benchmark.cu: add mpi_halo_exchange / host_staged_mpi scenarios — ring MPI_Sendrecv with host staging; transfer size controlled by --transfer-size-mb.
- bench/synthetic_cuda_benchmark.cu: add gpu_direct_rdma scenario — ring MPI_Sendrecv with device pointers (CUDA-aware MPI; requires --cuda-aware-mpi 1); paired with host_staged_mpi for inter-node staging comparison.
- bench/synthetic_cuda_benchmark.cu: add p2p_nvlink scenario — cudaDeviceEnablePeerAccess + cudaMemcpyPeer between --device and --peer-device; checks cudaDeviceCanAccessPeer and fails with a clear message if NVLink/PCIe P2P is unavailable.
- bench/synthetic_cuda_benchmark.cu: add p2p_staged scenario — forced D2H → H2D through pinned host buffer between two GPUs; paired with p2p_nvlink for intra-node staging comparison.
- bench/synthetic_cuda_benchmark.cu: add --transfer-size-mb, --peer-device, --cuda-aware-mpi, --stagger-us knobs; update ground-truth JSON with mpi_ranks field and new scenario bottleneck labels.
- bench/synthetic_cuda_benchmark.cu: refactor event handling into EventPair RAII struct; all scenarios now record both start and stop unconditionally with elapsed_ms guarded by timed flag.
- bench/Makefile: add mpi and mpi_nvtx targets using -DUSE_MPI -ccbin mpicxx.
- Add rank= parameter to detect_phases/compute_profile_summary; multi-rank --verbose output now prefixes each line with [phase rank=N] so output from different ranks can be distinguished.
- Fix _profile_bounds: include OSRT_API and MPI event tables (MPI_OTHER_EVENTS, MPI_COLLECTIVES_EVENTS, MPI_P2P_EVENTS) so t0 reflects the true profile start (first host-side event, e.g. MPI_Init_thread) rather than the first CUDA activity. Shifts t0 from 1.739s to 0.347s on the test profile, fixing phase table start/end times.
- Fix _fingerprint: kernel_ns now sums ALL kernels overlapping the window (overlap condition, clamped) rather than the dominant kernel's time only, so gpu_util is accurate for similarity scoring. Dominant kernel now uses center-of-kernel to assign ownership rather than strict containment, so boundary-straddling kernels no longer vanish from both adjacent segments.
- Fix phase detection bug: candidates within tolerance_ns of t0 or t1 were creating tiny phantom segments at profile edges because t0/t1 are appended after _merge_nearby and never participate in its averaging. Fix: drop candidates too close to either edge before merging.
- Add --verbose phase detection debug output: detect_phases() now accepts verbose=False and prints step-by-step diagnostics (bounds, raw candidates, post-merge boundaries, initial segment fingerprints, each greedy merge, final labels). compute_profile_summary() threads verbose through. Both analyze and summary CLI commands activate it via --verbose.
- Docs: add todo item 7 — threading for concurrent operations (multi-rank ingestion and summary via ThreadPoolExecutor, --workers flag).
- Cleanup: remove dead _batch_window_mpi_ops() from metrics.py; superseded by _compute_all_mpi_stats() and never called. Also cleaned up two docstring references to it.
- Security: remove dead NsysProfile.query_df() method; it was never called, bypassed the 50,000-row cap on query(), and unnecessarily exposed an unbounded pandas read path.
- Security: exclude profile_path from ProfileSummary.model_dump_json() when building LLM prompts (claude_code backend and all compare backends); the field is not needed by the model and discloses the user's local filesystem path to the LLM API provider.
- Security: exclude profile_a_name/profile_b_name from ProfileDiff.model_dump_json() in the compare LLM prompt; filenames are now shown to the user in the terminal via a new "Profile A: / Profile B:" label printed before the phase tables, but kept out of the model context.
- Security: cap NsysProfile.query() at 50,000 rows with a stderr warning if truncated; prevents unbounded memory use from unexpectedly large result sets.
- Security: add LIMIT 50,001 guard and stderr warning to _mpi_cluster_boundaries() in phases.py; prevents OOM from profiles with abnormally high MPI_Barrier counts.
- Fix: correct Gemini optional dependency in pyproject.toml from google-generativeai to google-genai (the package that provides the google.genai import used in the code).
- Security: remove profile filenames from LLM prompts (analyze and compare) to eliminate a prompt-injection surface via user-controlled filenames.
- Fix: remove incorrect PRAGMA table_info() suggestion from _SQL_SCHEMA_REFERENCE system prompt; sql_query blocks PRAGMA, so the instruction was misleading and wasted agent turns.
- Security: remove dead `resolve_enum()` method from NsysProfile (unreachable code with latent SQL injection via unvalidated table name).
- Security: add allowlist check in `NsysProfile.columns()` before PRAGMA table_info — validates table name against schema before interpolating into SQL.
- Security: document prompt injection risk from untrusted profile data in README § Risks, both subcommand --help descriptions, the analyze/compare system prompts, and the claude_code fallback prompt.
- Security: catch OSError in both claude_code subprocess paths (loop.py, compare.py) to surface a clear error message when the profile summary exceeds the OS argument-length limit (ARG_MAX).
- Fix missing phase detection and metrics timing in multi-rank mode: accumulate
  per-rank timings into the shared dict so the timing breakdown table is complete.
- Add MIT LICENSE file (pre-release legal requirement).
- Add project metadata to pyproject.toml: description, license, authors, classifiers,
  and [project.urls] pointing to https://github.com/leonhostetler/PerfAdvisor.
- Update .gitignore: remove LEON.md and other_ideas.md (no longer in this directory);
  add *.sqlite, *_log.txt, and .env.
- Add Data Privacy section to README near the top, and data privacy notice to the
  `analyze` and `compare` subcommand descriptions (visible in --help output).
- Add user-facing error handling: clean error message for missing/invalid SQLite profile
  (validated in NsysProfile.__init__); clean RuntimeError with env var instructions for
  missing ANTHROPIC_API_KEY or OPENAI_API_KEY; top-level FileNotFoundError/ValueError
  handler in main() for all profile-related errors.
- Update README: expand intro blurb to list multi-rank, comparison, and multi-provider
  capabilities; add --max-phases, --log-file, --transcript-file to compare usage examples;
  add tested platform note (Ubuntu 24.04.4 LTS, Nsight Systems 2025.3.1.90, schema 3.20.2).

2026-04-08
----------
- Update README: add Multi-rank analysis subsection under Usage, update pre-seed
  note to mention cross_rank_summary, update token usage table with multi-rank
  overhead estimate.

- Implement multi-rank MPI analysis (todo #1): `analyze` now accepts multiple
  .sqlite files (or a shell glob) to enable cross-rank analysis alongside the
  standard single-rank agent loop.
  - New `perf_advisor/analysis/cross_rank.py`: `parse_rank_ids` (varying-slot
    integer detection with index-order fallback), `select_primary_rank` (GPU
    idle outlier detection, 20% threshold), `align_phases` (name-match /
    index-order / failed modes with duration-tolerance check),
    `compute_cross_rank_summary` (per-phase GPU kernel and MPI wait imbalance
    scores, per-collective breakdown, per-rank raw stats).
  - New Pydantic models in `models.py`: `RankOverview`, `RankPhaseStats`,
    `CollectiveImbalance`, `CrossRankPhaseSummary`, `CrossRankSummary`.
  - `__main__.py`: `profile` argument changed to `nargs='+'`; `--primary-rank N`
    flag added; multi-rank path prints per-rank overview table and per-phase
    imbalance table with color-coded imbalance scores; phase alignment failures
    produce prominent Rich Panel warnings and fall back to single-rank analysis.
  - `agent/loop.py`: all four backends (`_run_api`, `_run_openai`, `_run_gemini`,
    `_run_claude_code`) and `run_agent` accept optional `cross_rank_summary`;
    injected as a third pre-seeded tool result (`pre_3`) in the Anthropic and
    OpenAI backends; appended to the initial message in Gemini and claude_code.
  - New `tests/test_cross_rank.py`: 18 unit tests covering all four functions,
    all passing with no real profile files required.


- Update README Development section: rename to Testing, update pytest invocation
  to `python -m pytest`, update single-test example to test_synthetic.py, add
  one-sentence summary about the synthetic fixture.

- Add portable test fixtures (todo item 11): create tests/conftest.py with a
  session-scoped synthetic SQLite fixture covering all core metric functions
  (kernels, memcpy, gaps, streams, NVTX, MPI, device info, CPU sync, tool
  dispatch); add tests/test_synthetic.py with 34 fast tests that run in CI
  without the real Nsight profile or an API key; remove the old
  test_ingestion.py and test_analysis.py which were tied to a hardcoded
  local profile path and offered no coverage beyond the synthetic suite.

- Rename project from nsight-agent / nsight_agent to perf-advisor / perf_advisor:
  rename package directory, update pyproject.toml name and script entry point,
  update all imports, CLI prog= string, README, todo_list.md, changelog header,
  and .gitignore comment; update README title to PerfAdvisor and broaden
  description to frame the tool as a general GPU/accelerator profiler with
  Nsight Systems as the current backend and ROCProfiler planned; broaden
  perf_advisor/__init__.py docstring to match
- Add `phase` and `evidence` columns to the hypotheses rich table in __main__.py;
  phase appears after Type, evidence appears after Description
- Add `phase` field to hypothesis schema (_HYPOTHESIS_SCHEMA): LLM must now cite
  the exact phase name from phase_summary results (or "whole_profile") for each
  hypothesis; makes phase attribution unambiguous without requiring readers to infer
  it from evidence text; added note that phase names are already in the pre-seeded
  data so the LLM should not issue SQL queries to determine phase membership
- Fix sql_query SELECT-only check to strip -- and /* */ comments before the
  keyword check; LLM-generated queries often include a leading -- comment line
  which caused legitimate SELECT queries to be incorrectly rejected
- Add 30s automatic timeout to sql_query: a threading.Timer sets the same stop
  event used by Ctrl-C; a separate timed_out flag distinguishes the two sources
  so the LLM receives an actionable message ("Query timed out after 30s — avoid
  self-joins / correlated subqueries, scope with timestamps, or use a structured
  tool") rather than a bare "interrupted"
- Add sql_query guardrails to prevent runaway queries hanging the process:
  (1) SELECT/WITH-only filter — blocks PRAGMA, ATTACH, and write statements;
  (2) LIMIT injection — appends LIMIT 200 to queries that omit it;
  (3) SQLite progress handler in NsysProfile.query_safe() fires every 1000 VM
      instructions and raises OperationalError if a threading.Event is set,
      making every query interruptible; SIGINT is wired to that event so Ctrl-C
      works even while SQLite is running in its C extension;
  (4) 100 KB byte cap — result dicts over 100 KB are blocked with an actionable
      error message telling the LLM to narrow its query or use a structured tool;
  all four guardrails apply to all three backends (Anthropic, OpenAI, Gemini)
  via the shared dispatch() / tool_sql_query() path
- Add SQL schema reference block (_SQL_SCHEMA_REFERENCE) to the multi-turn system
  prompt: lists exact column names for CUPTI_ACTIVITY_KIND_KERNEL (start/end/shortName/
  demangledName/grid/block/registers/sharedMem/streamId/correlationId),
  CUPTI_ACTIVITY_KIND_MEMCPY (start/end/bytes/copyKind), CUPTI_ACTIVITY_KIND_RUNTIME
  (start/end/nameId/correlationId), all three MPI tables (start/end/textId),
  NVTX_EVENTS (start/end/text/eventType), and StringIds (id/value); eliminates
  the schema-discovery turns observed in log analysis
- Add SQLite-compatibility note to system prompt and sql_query tool description:
  PERCENTILE_CONT, MEDIAN, STDDEV are not supported; prevents wasted calls observed
  in log (model tried PERCENTILE_CONT which failed)
- Add no-duplicate-calls instruction to system prompt: "before calling a tool, check
  whether you have already called it with the same arguments"
- Add get_table_schema tool (wraps PRAGMA table_info): returns column list for any
  profile table; more efficient than SELECT * LIMIT 1 for schema discovery
- All four changes apply only to multi-turn backends (Anthropic/OpenAI/Gemini) via
  _SYSTEM_PROMPT_API_BASE and tool_schemas(); the single-shot claude_code fallback
  is unaffected
- Replace unconditional *_prompt.txt / *_response.txt / *_output.txt file saving
  with opt-in --log / --log-file and --transcript / --transcript-file flags on
  analyze and compare subcommands; no files are written to disk unless requested
- Add perf_advisor/agent/logger.py: LLMLogger context manager that writes a
  structured log of every API request and response in real time (with flush after
  each write so partial logs are valid on failure); header includes command, argv,
  provider, model, profile path, and start timestamp
- Thread LLMLogger through run_agent() and all _run_* backends (Anthropic, OpenAI,
  Gemini, Claude Code); each backend logs full request payload and full response
  before/after every API call including the forced-output final turn
- For Gemini, log the system instruction, tool declaration names, and message
  content explicitly (since the chat object manages history internally)
- Remove _save_files() helper from loop.py; remove post-hoc prompt reconstruction
- Rename auto-saved files: *_transcript.txt (was *_output.txt), *_log.txt
  (replaces *_prompt.txt + *_response.txt)
- Update README: document new --log, --log-file, --transcript, --transcript-file
  flags for both analyze and compare; update Risks section

2026-04-07
----------
- Fix preflight token overestimate for OpenAI/Gemini: `estimate_json_tokens` now accepts
  a `provider` parameter with calibrated chars/token ratios (anthropic: 2.5, openai/gemini: 3.5);
  `cmd_analyze` and `cmd_compare` pass `resolved_provider` through
- Show input token estimate as a range (5 turns → max_turns) for OpenAI/Gemini rather than
  a single worst-case figure; range collapses to a single number when max_turns ≤ 5
- Add per-turn context size display to OpenAI and Gemini backends (matching Anthropic):
  OpenAI uses `response.usage.prompt_tokens` and `prompt_tokens_details.cached_tokens`;
  Gemini uses `usage_metadata.prompt_token_count` and `cached_content_token_count`
- Fix OpenAI backend multi-turn bug: when `finish_reason == "stop"` but the model outputs
  prose instead of a JSON array, the loop now injects a recovery prompt and continues rather
  than returning `[]` immediately after a single turn
- Fix OpenAI backend file saving: prompt and response are now saved unconditionally on stop,
  not only when hypotheses are successfully extracted
- Remove rate-limiting feature (`--rate-limit-tpm`, `_rate_limit_sleep`, `_token_window`,
  `rate_limit_tpm` param from `_run_api` and `run_agent`): Anthropic's rate limit does
  not apply to cache reads; with sliding cache the non-cached increment per turn is only
  ~1–3k tokens, making the rate limit effectively unreachable in normal use
- Update per-turn context display in `_run_api`: shows `Context size ≈ X,XXX tokens
  (Y,YYY cached)` from turn 2 onwards, where cached ≈ previous turn's total context;
  turn 1 shows no cached count since nothing is in cache yet
- Add `--rate-limit-tpm` flag to `analyze`: when set, `_run_api` tracks a 60-second
  rolling token window (deque of monotonic timestamp + exact context size from
  `response.usage`) and calls `_rate_limit_sleep` before each turn; `_rate_limit_sleep`
  loops until the window has room, sleeping the exact number of seconds until the oldest
  entry expires and printing a notification to the user each time it sleeps; single
  requests that alone exceed the limit are passed through without sleeping; no-ops for
  non-Anthropic providers; `run_agent` gains `rate_limit_tpm` parameter
- Add per-turn context size display to `_run_api`: prints `Context size ≈ X,XXX tokens`
  before each API call (turn 1 uses heuristic estimate from messages + schemas + system
  prompt; subsequent turns use exact prior context + output + tool result estimate)
- Move turn header display to before the API call in `_run_api` so it appears with the
  context size line rather than after the (potentially long) API wait
- Fix `cache_control` 4-block limit error and restore true sliding cache in `_run_api`:
  keep the 2 most-recent per-turn user messages marked (`_cache_prev` + `_cache_pprev`),
  stripping only the oldest on each advance; total markers stay at 4 (system + preseed +
  2 floating), which is Anthropic's maximum; this gives true sliding cache where turn N
  reads everything through turn N-1 from cache (0.10×) rather than only the pre-seeded
  content
- Add Anthropic sliding prompt cache to `_run_api` in `loop.py`: system prompt and
  pre-seeded profile summary marked with `cache_control: ephemeral` on turn 1 (cache
  write at 1.25×); each subsequent turn marks the last tool-result block to extend the
  cached prefix (cache read at 0.10× for all prior content, cache write for the new
  increment); result is ~75–80% reduction in billable input tokens on a typical multi-turn
  run; `cache_creation_tokens` and `cache_read_tokens` tracked per turn and summed
- Return `_run_api`, `_run_openai`, `_run_gemini` as 5-tuples
  `(hypotheses, input, cache_creation, cache_read, output)`; `run_agent` populates
  `token_usage` with `cache_creation_tokens` and `cache_read_tokens`; OpenAI and
  Gemini return 0 for both cache fields
- Add `estimate_total_session_tokens` and `estimate_cache_breakdown` to `preflight.py`:
  `estimate_total_session_tokens` computes total input across N stateless turns
  (N×initial + increment×N(N-1)/2); `estimate_cache_breakdown` computes expected
  cache_creation, cache_read, and cost-equivalent for Anthropic sliding cache
- Update `cmd_analyze` preflight display: Anthropic shows cache_write / cache_read /
  cost-equiv breakdown; other providers show total session estimate with a note that
  OpenAI applies automatic ~50% caching
- Update `cmd_analyze` post-run token display: when cache fields are non-zero, show
  non-cached / cache-write / cache-read / out separately with total and cost-equivalent;
  fall back to existing `in / out` display for non-caching providers
- Update README: add "Prompt caching" subsection explaining per-provider caching
  behavior; update "Pre-flight estimate" with new display format examples; add note to
  "Minimizing token expenditure" that Anthropic caching is automatic
- Update README: document `--max-turns` flag, wrap-up warning, and forced final
  turn behavior in Useful Flags and Minimizing Token Expenditure sections;
  add note to Pre-flight Estimate section that output range reflects `--max-turns`
- Add turn-limit safety net to agent loop (`loop.py`): instead of raising
  `RuntimeError` when `max_turns` is exhausted, inject a wrap-up warning
  `WARN_TURNS_BEFORE_LIMIT` (3) turns before the limit (bundled with tool
  results so no extra API call); on the final turn, bundle the forced-output
  prompt with tool results; after the loop, make one additional API call
  without tools to extract whatever hypotheses the model has accumulated;
  applies to all three backends (Anthropic, OpenAI, Gemini); Gemini uses
  `Part.from_text` for the warning and a standalone `chat.send_message` for
  the forced turn; returns `[]` (with a warning log) rather than raising if
  no hypotheses are produced even after the forced turn
- Add `--max-turns` flag to `analyze` subcommand: overrides `MAX_TURNS=20`
  default; wired through to `run_agent`; preflight output/range display
  uses the resolved value rather than the hardcoded constant
- Improve pre-flight token estimate accuracy: replace single char/4 heuristic
  with content-aware `estimate_prose_tokens` (4 chars/token) and
  `estimate_json_tokens` (2.5 chars/token) in `preflight.py`; `analyze` applies
  prose ratio to the system prompt and JSON ratio to summary + tool schemas;
  `compare` applies prose ratio to the system prompt and JSON ratio to the
  compare prompt (which is ~95% JSON); reduces underestimate from ~34% to ~5%
  on a real profile comparison run (13k estimated → 21k vs 19,771 actual)
- Add pre-flight token estimate to `analyze` and `compare` subcommands: after
  `compute_profile_summary()` (analyze) or `compute_profile_diff()` (compare) and
  before the LLM call, print input/output token estimates and prompt for confirmation;
  confirmation is skipped when `--yes`, `--quiet`, `--json`, or stdin is non-interactive;
  estimate is suppressed only by `--quiet` or `--json`
- Add `--yes` flag to `analyze` and `compare`: skip confirmation prompt
- Add `--exact-token-count` flag to `analyze` and `compare`: use Anthropic's
  `client.messages.count_tokens()` API for an exact input count instead of the char/4
  heuristic; no-ops with a note for non-Anthropic providers
- Add `perf_advisor/agent/preflight.py`: `estimate_tokens_heuristic(*texts)` and
  `count_tokens_exact(provider, model, system_prompt, *user_texts, tools=None)`
- Consolidate `--provider` flag into `--model`: `_parse_provider_and_model` now
  accepts only the model string; a bare provider name (e.g. `--model=openai`)
  is treated as a provider-only shorthand that selects the default model for
  that provider; the explicit `--provider` flag is removed from both `analyze`
  and `compare` subparsers; `run_agent` and `run_compare` signatures updated
  accordingly; README Provider Selection section updated with new examples

2026-04-07
----------
- Add `compare` subcommand: single-shot LLM comparison of two `.sqlite` profiles
  producing a structured `ComparisonReport` (narrative + key differences table
  ordered by magnitude); no tool-use loop — both summaries and a pre-computed
  structural diff are injected into one prompt
- Add `perf_advisor/analysis/diff.py`: `compute_profile_diff()` aligns two
  `ProfileSummary` objects and determines comparison mode:
    - `phase_aware` — same phase count + names; full per-phase analysis
    - `summary` — phases differ but Jaccard kernel overlap >= 20%; per-kernel diff included
    - `summary_no_kernel` — phases differ and overlap < 20%; top-level metrics only
  Jaccard similarity used for kernel overlap; status message shown before LLM call
  (e.g. "Phases do not match (A has 4, B has 6) and kernel overlap is 8%...")
- Add `perf_advisor/agent/compare.py`: `run_compare()` single-shot backends for
  Anthropic, OpenAI, Gemini, and claude_code; saves `{stem_a}_vs_{stem_b}_{ts}_prompt.txt`
  and `_response.txt` alongside the profile
- Add `ScalarDiff`, `KernelDiff`, `MemcpyDiff`, `MpiDiff`, `ProfileDiff`,
  `ComparisonDiff`, `ComparisonReport` Pydantic models to `analysis/models.py`
- Add `_print_phase_table()` helper to `__main__.py`; `compare` always prints both
  phase tables before the LLM call; key differences table color-codes by magnitude
- Output saved to `{stem_a}_vs_{stem_b}_{ts}_output.txt` next to profile A

- Save terminal output to `{stem}_{ts}_output.txt` alongside prompt/response files
  after each `analyze` run: `Console(record=True)` captures all Rich table output;
  a `log` callable is threaded through `run_agent` and all four backends so the
  turn-by-turn agent output (previously raw `print()`) is routed through the same
  console and captured; output file is not written in `--json` mode

- Remove `_ensure_indexes` and the writable connection in `NsysProfile`: the tool
  previously opened a writable connection on first use to add query-accelerating
  indexes to the profile file; SQLite does not support temporary indexes on
  non-temporary tables, so there is no non-modifying alternative; the ~25% local
  analysis speedup was not worth the hidden file modification given that runtime
  is dominated by LLM round-trips
- Add `has_table` guards to `compute_gpu_sync_time` and `compute_gpu_memcpy_time`:
  previously both would crash with `OperationalError` on profiles that lack
  `CUPTI_ACTIVITY_KIND_SYNCHRONIZATION` or `CUPTI_ACTIVITY_KIND_MEMCPY`
- Add docstring note to `PhaseSummary` clarifying that `total_gpu_idle_s` counts
  only inter-kernel gaps, not leading/trailing idle at phase boundaries
- Add "Provenance" section to README noting the tool was vibe coded and that
  metrics should be treated as starting points rather than ground truth

2026-04-06
----------
- Add phase-scoped time window support to agent tools: `top_kernels`, `gap_histogram`,
  `memcpy_summary`, `mpi_summary`, `nvtx_ranges`, and `stream_summary` now accept optional
  `start_ns`/`end_ns` parameters; when provided they query only within that window using
  existing internal `_window_*` helpers; tool schemas updated with parameter descriptions
  that direct the agent to use phase timestamps from the pre-seeded context
- Add `_window_memcpy_by_kind`, `_window_streams`, `_window_nvtx_ranges` to `metrics.py`
  (windowed counterparts for the three functions that previously lacked them)
- Add `start_ns`/`end_ns` fields to `PhaseSummary` (absolute CUPTI nanosecond timestamps);
  the pre-seeded phase summary previously only had `start_s`/`end_s` (seconds), which the
  agent could not use directly as tool window arguments
- Add `--allow-app-knowledge` flag to `analyze`: by default the system prompt
  now constrains the model to ground all suggestions in profile data only;
  `--allow-app-knowledge` restores the previous behavior where the model may
  draw on training knowledge about the application (e.g. suggest named env vars
  inferred from kernel names)
- Full kernel name resolution (todo item 2): switch GROUP BY from shortName to
  demangledName so each template instantiation is a distinct row; add
  `_normalize_demangled()` to strip SFINAE prefix and `(T2)` suffix; add
  `short_name` field to `KernelSummary` for CLI display; fall back to shortName
  on older profiles that lack the demangledName column
- Add `action_category` field to hypothesis schema (todo item 5): enum of
  `runtime_config` / `launch_config` / `code_optimization` / `algorithm`,
  ordered by effort; boundary rule (what vs. how) included in system prompt
- Add "Action" column (magenta, abbreviated) to hypotheses table in `cmd_analyze`
- Update `README.md`: document `action_category` field in hypothesis output
- Add `eval/score.py` — standalone independent hypothesis evaluator; scores
  perf-advisor hypothesis JSON using gpt-5.4 on six criteria (bottleneck
  alignment, addressability, mechanistic plausibility, specificity/actionability,
  testability, generality) plus implementation-cost and correctness-risk flags
  and a list-level redundancy score; writes `.eval.json` alongside input
- Add `eval/context_example.yaml` — template for injecting application/phase
  context into the evaluator prompt (critical for addressability scoring)
- Add `eval/` to `.gitignore` — evaluator is intentionally separate from perf-advisor
- Add rich terminal table to `eval/score.py` — displays per-hypothesis scores (color-coded
  1–5), implementation-cost and correctness-risk flags, and per-hypothesis average; footer
  shows redundancy score and overall aggregate
- Update `eval/score.py` table and output files: rename column "Hypothesis" → "Suggestion"
  (shows verbatim suggestion text truncated to 50 chars); save full LLM prompt to
  *.eval.prompt.txt; save plain-text table + rationales to *.eval.table.txt
- Add verdict + knowledge_source to `eval/score.py` — LLM now gives an explicit
  strongly_recommended/recommended/uncertain/not_recommended verdict per hypothesis with
  a 2-3 sentence rationale; knowledge_source field (context/model/both) flags when the
  verdict draws on the model's own training knowledge of the application vs. injected context;
  verdict column added to the rich table with rationales printed below; prompt strengthened
  to be opinionated and treat injected context as authoritative

2026-04-04
----------
- Fix richer output table for phases/analysis in `cmd_analyze` (c8c6fba)
- More useful information printed when running `analyze` command (bc95cb0)
- Replace deprecated `google.generativeai` package with `google.genai` (dbf7144)
- Add support for newer OpenAI models that report `max_completion_tokens`
  instead of `max_tokens` (4b4f74b)
- Check for existence of prerequisite packages (openai, google-genai) at
  runtime and fix default model names per provider (4a0fc4f)

2026-04-02
----------
- Add token reporting: track and display prompt/completion token counts
  after each agent run (4748cfc)
- Add README.md (fdb639e)
- Multi-provider LLM support (OpenAI, Gemini) (73ed731)
  - `_parse_provider_and_model()` resolves backend from model prefix, explicit
    --provider flag, or env-var auto-detection (ANTHROPIC → OPENAI → GOOGLE)
  - `_run_openai()` — function-calling loop via openai>=1.0
  - `_run_gemini()` — Gemini start_chat() loop via google-genai
  - `--provider {anthropic,openai,gemini}` flag added to CLI
  - New optional extras: `pip install 'perf-advisor[openai]'` / `[gemini]`
- CPU-GPU overlap metrics (4d6c67f)
  - `KernelSummary`: avg/max launch overhead (µs) per kernel name
  - `ProfileSummary`: `cpu_sync_blocked_s` / `cpu_sync_blocked_pct`
  - Sample (A100 CG run): 20.5% of GPU kernel time spent in cuEventSynchronize
- Per-phase gap histogram (f88088d)
  - `PhaseSummary.gap_histogram` now scoped to phase window (6 buckets:
    <10µs through >100ms); previously only a global histogram existed
- Make PRAGMA cache_size and mmap_size adaptive based on profile file size (4676d36)
- Richer GPU metrics (6329ff0)
  - `KernelSummary`: std_dev_ms, cv, avg_registers_per_thread,
    avg_shared_mem_bytes, estimated_occupancy
  - `MemcpySummary`: pct_of_peak_bandwidth
  - `ProfileSummary`: peak_memory_bandwidth_GBs (from TARGET_INFO_GPU)
- Runtime performance improvements — analysis time reduced ~25% (200b108)
  - Index creation on first open for kernel start and MPI tables
  - Single-pass MPI stats with CASE-based conditional aggregation
  - Self-profiling timers printed in timing breakdown table

2026-04-01
----------
- Profile segmentation into logical compute phases (fcfedc3)
- Save AI prompt and response to file for inspection / replay (22862cf)
- Add --verbose flag to print ProfileSummary before sending to AI (19b8543)
- Initial commit: ingestion, analysis, Claude agent loop, CLI (bcf1b2c)
